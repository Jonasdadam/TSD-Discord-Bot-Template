const { SlashCommandBuilder, PermissionFlagsBits, EmbedBuilder, ThreadAutoArchiveDuration, ActionRowBuilder, ButtonBuilder, ButtonStyle, MessageFlags } = require("discord.js");

module.exports = {
	customId: "example_btn",
	userPermissions: [],
	botPermissions: [],

	run: async (client, interaction) => {
		
	},
};
